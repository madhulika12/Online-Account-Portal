'use strict';

angular.module('ssoApp')
    .controller('test', ['displayResponseBox','$state','tokenStorageService', '$rootScope', 'loadBrandingService', '$http', '$scope', '$timeout', function(displayResponseBox, $state, tokenStorageService, $rootScope, loadBrandingService, $http, $scope, $timeout) {
        // console.log("test");
    }])
